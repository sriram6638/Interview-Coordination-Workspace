runtime: nodejs20

env: standard
instance_class: F2

entrypoint: npm start

env_variables:
  NODE_ENV: production

automatic_scaling:
  min_instances: 1
  max_instances: 10

handlers:
  - url: /.*
    script: auto
